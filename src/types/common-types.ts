export type CommonOption = {
    name: string;
    val: string;
}

export type MoviesFilterType = {
    search: string;
    quality: string;
    genre: string;
    rating: string;
    year: string;
    language: string;
}